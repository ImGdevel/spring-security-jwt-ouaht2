# Local environment variables (copy to `.env` and fill in values)

# Enable oauth2 registrations (loads `application-oauth2.yaml`)
SPRING_PROFILES_ACTIVE=oauth2

# OAuth2 providers
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Optional: Redis (if you want to override application.yaml)
# SPRING_REDIS_HOST=localhost
# SPRING_REDIS_PORT=6379
